<%= form_with(model: @developer, local: true) do |form| %>
  <% if @developer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@developer.errors.count, "error") %> prohibited this developer from being saved:</h2>

      <ul>
      <% @developer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :admin, t('developer.role') %>
    <%= form.select :admin, [[t('developer.admin'), true], [t('developer.developer'), false]] %>
  </div>

  <div class="field">
    <%= form.label :team_id, t('developer.team') %>
    <%= form.select :team_id, @teams.collect { |team| [team.name, team.id] }  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
